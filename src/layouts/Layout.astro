---
import "../styles/global.css";
import Header from "../components/Header.astro";

interface Props {
  title: string;
  description?: string;
  image?: string;
  type?: "website" | "article";
  publishedDate?: Date;
  headline?: string;
}

const {
  title,
  description = "Damian Bednarczyk - Mathematics undergraduate and software developer specializing in computational mathematics.",
  image = "/android-chrome-96x96.png",
  type = "website",
  publishedDate,
  headline,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, "https://bednarczyk.xyz");
const imageURL = new URL(image, "https://bednarczyk.xyz");

const author = {
  "@type": "Person",
  name: "Damian Bednarczyk",
  url: "https://bednarczyk.xyz",
  sameAs: [
    "https://github.com/dkbednarczyk",
    "https://www.linkedin.com/in/dkbednarczyk",
  ],
};

const jsonLd =
  type === "article"
    ? {
        "@context": "https://schema.org",
        "@type": "BlogPosting",
        headline: headline ?? title,
        description,
        url: canonicalURL.href,
        image: imageURL.href,
        inLanguage: "en",
        mainEntityOfPage: {
          "@type": "WebPage",
          "@id": canonicalURL.href,
        },
        author,
        ...(publishedDate && { datePublished: publishedDate.toISOString() }),
      }
    : {
        "@context": "https://schema.org",
        "@type": "WebSite",
        name: "Damian Bednarczyk",
        description,
        url: canonicalURL.href,
        inLanguage: "en",
        author,
      };
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="generator" content={Astro.generator} />
        <meta name="description" content={description} />
        <meta name="author" content="Damian Bednarczyk" />
        <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
        
        <!-- Canonical URL -->
        <link rel="canonical" href={canonicalURL.href} />
        
        <!-- Open Graph / Facebook -->
        <meta property="og:type" content={type} />
        <meta property="og:url" content={canonicalURL.href} />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:image" content={imageURL.href} />
        <meta property="og:image:alt" content="Damian Bednarczyk" />
        <meta property="og:locale" content="en_US" />
        <meta property="og:site_name" content="Damian Bednarczyk" />
        {type === "article" && publishedDate && <meta property="article:published_time" content={publishedDate.toISOString()} />}
        {type === "article" && <meta property="article:author" content="https://bednarczyk.xyz" />}
        
        <!-- Twitter -->
        <meta name="twitter:card" content="summary" />
        <meta name="twitter:url" content={canonicalURL.href} />
        <meta name="twitter:title" content={title} />
        <meta name="twitter:description" content={description} />
        <meta name="twitter:image" content={imageURL.href} />
        <meta name="twitter:image:alt" content="Damian Bednarczyk" />
        
        <link rel="alternate" type="application/rss+xml" title="Damian Bednarczyk Blog" href="/rss.xml" />
        
        <!-- Structured Data -->
        <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
        
        <title>{title}</title>
        <link
            rel="apple-touch-icon"
            sizes="120x120"
            href="/apple-touch-icon.png"
        />
        <link rel="icon" type="image/x-icon" href="/favicon.ico" sizes="any" />
        <link
            rel="icon"
            type="image/png"
            sizes="32x32"
            href="/favicon-32x32.png"
        />
        <link
            rel="icon"
            type="image/png"
            sizes="16x16"
            href="/favicon-16x16.png"
        />
    </head>
    <body>
        <Header />
        <main>
            <slot />
        </main>
    </body>
</html>
