---
type Props = {
    project: {
        title: string,
        url: string,
        description: string
    }
}

const { project } = Astro.props
---

<td>
    <a href={project.url}>{project.title}</a>
    <br>
    <p style="min-width: 10em;">{project.description}</p>
</td>
